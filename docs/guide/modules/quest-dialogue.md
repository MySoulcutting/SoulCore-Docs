# CustomQuest 任务对话

SoulCore `1.4.0` 可把配套 CustomQuest 服务端选出的 NPC 对话显示为沉浸式客户端界面。世界画面保持可见，正文逐字出现，玩家选择通过一次性会话安全回传。

**模块状态**：客户端 · 可选集成 CustomQuest · 不依赖 SoulCore Paper 插件

## 使用前提

- 客户端安装 SoulCore NeoForge `1.4.0` 或同协议兼容版本。
- Paper 服务端安装支持 SoulCore 任务对话通道的 CustomQuest。
- NPC、对话、任务条件和点击动作均在 CustomQuest 中配置；SoulCore 没有单独的对话配置文件。

## 界面行为

- 打开对话时世界画面仍可见，游戏不暂停，也不会出现全屏菜单模糊背景。
- 画面下方靠中位置显示渐隐暗幕、NPC 标题和自动换行正文。
- 正文按 Unicode 字符逐字播放打字机效果，服务端提供的 legacy `§` 颜色和样式会保留。
- 标题右上方最多显示 6 个无底框文字选项；选项带 2 像素阴影，悬停或键盘聚焦时变为金色。
- 服务端提供的 hover 文本作为 Tooltip 显示。
- 小窗口会自动收紧内容宽度、减少正文可见行或把选项改为多列，优先保证选项仍可操作。

## 操作方式

- 对话打开后鼠标会直接解锁，不需要先按 `T`。
- 可使用鼠标、Tab、方向键与确认键选择选项。
- 点击后所有选项立即禁用，等待服务端打开下一段或关闭当前会话，避免重复提交。
- 按 `Esc` 会关闭界面并向服务端发送 dismiss。

## 服务端权威与生命周期

SoulCore 客户端只显示服务端提供的标题、正文与稳定选项 ID，不在本地判断任务分支或执行命令。CustomQuest 会重新验证：

- 玩家是否仍满足任务和对话条件；
- 会话是否在有效期内；
- 玩家与 NPC 的距离；
- 选项是否属于当前会话且尚未消费。

每次打开都使用独立会话 ID。迟到的旧关闭消息不会关掉新对话；重新加入、断线、切换世界或收到当前会话的关闭消息时会清理界面，不跨连接残留。

## 没有显示对话时

1. 确认 SoulCore 客户端版本至少为 `1.4.0`。
2. 确认 CustomQuest 已启用客户端对话集成，并且 NPC 对话配置有效。
3. 查看客户端 `logs/latest.log` 与服务端日志，确认没有通道版本或消息校验错误。
4. 使用旧客户端对照测试；旧客户端不会声明新通道，服务端会按 CustomQuest 自身的兼容策略处理。

## 相关

- [任务导航](/guide/modules/quest-navigation) —— 世界中的光柱、圆环和距离标签
- [任务追踪 HUD](/guide/modules/quest-tracking) —— 多任务进度与导航按钮
- [安装](/guide/installation) —— CustomQuest 可选集成说明
